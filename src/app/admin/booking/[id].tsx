/**
 * Admin booking detail — placeholder stub for Task 4.
 *
 * This file must exist so Expo Router registers the typed route
 * /admin/booking/[id] that the admin list screen navigates to.
 * Task 4 will replace this with the full detail UI.
 */

import { Text, View } from 'react-native';

export default function AdminBookingDetailScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Booking detail — coming in Task 4</Text>
    </View>
  );
}
