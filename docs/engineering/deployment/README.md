# Deployment

**Purpose:** How QuickServe is built, configured, and shipped — environments,
Expo/EAS builds, web export, database migration deployment, and Edge Function deploys.

**Planned documents:**
- Environments & configuration (env vars)
- App builds (Expo web export, EAS Android/iOS)
- Database migration deployment (`supabase db push`)
- Edge Function deployment
- Release/rollback procedure

**Verified references:** `docs/pilot/` release checklists; migration state in
`supabase/migrations/`. Document only procedures that have been executed and verified.
