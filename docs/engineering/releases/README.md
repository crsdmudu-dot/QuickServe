# Releases

**Purpose:** QuickServe release history — release candidates, changelogs, and the
state certified at each milestone.

**Planned documents:**
- Release candidate records (scope, migrations, certification results)
- Changelog / version history
- Known issues per release

**Verified reference:** the current backend Release Candidate baseline (QA Slice 44 +
RC1) is recorded in `qa/docs/LAUNCH-CERTIFICATION.md` and the git history on `main`.
Record only states that were actually verified and merged.
