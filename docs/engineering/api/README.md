# API

**Purpose:** The API surface QuickServe clients use — Supabase PostgREST table
endpoints, RPC functions, Auth endpoints, and Edge Function HTTP endpoints, with
their request/response contracts.

**Planned documents:**
- REST/PostgREST endpoint reference (bookings, profiles, notifications, …)
- RPC reference (analytics, wallet, promotions, quality, …)
- Edge Function HTTP contracts
- Auth endpoints and token usage

Document only endpoints and shapes that have been verified against the backend.
