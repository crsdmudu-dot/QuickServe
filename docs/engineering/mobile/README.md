# Mobile

**Purpose:** The QuickServe React Native mobile applications — the Customer app and
the Provider app (Expo Router), including native concerns.

**Planned documents:**
- Customer app overview (`src/app/(customer)/`, `src/app/booking/`)
- Provider app overview (`src/app/provider/`)
- Native concerns (camera/evidence, location/tracking, push)
- Expo/EAS build notes

Document only implemented and verified screens/flows; cross-link to `deployment/`.
