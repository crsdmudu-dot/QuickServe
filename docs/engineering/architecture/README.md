# Architecture

**Purpose:** System-wide architecture of QuickServe — the components, their
boundaries, and how data flows between them (Customer app, Provider app, Admin web,
Supabase backend, and Edge Functions / external services).

**Planned documents:**
- System overview & component map
- Data flow (booking lifecycle across roles)
- Service boundaries and integration points
- Technology stack and key decisions

Document only verified structure; link to `docs/superpowers/specs/` for design history.
