# Backend

**Purpose:** Server-side business logic and behavior — the TypeScript data-access
libraries (`src/lib/`), the Supabase Edge Functions (`supabase/functions/`), and the
rules they enforce.

**Planned documents:**
- Booking lifecycle & dispatch logic
- Data-access library reference (`src/lib/*`)
- Edge Functions reference (M-Pesa, push, places, tracking)
- Server-side validation & error handling

Describe only verified behavior; cross-link to `database/` for RLS-enforced rules.
